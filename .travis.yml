language: ruby
before_install:
  - gem install bundler

rvm:
  - 2.0.0

script: 'bundle exec rake'

notifications:
  email:
    recipients:
      - tanay@genii.in
    on_failure: change
    on_success: never